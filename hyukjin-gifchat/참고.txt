웹소컷의 작동 순서.
1. html 의 스크립트가 실행된다.
2. 스크립트 중에 emit 이 있으면 실행된다
3. 백에 프론트가 최초 연결될때 
백에서 socket emit 는 최초 연결시에는 실행되지 않지만
socket on 은 프론트에서 emit 요청이 있다면 실행된다.

한 브라우저는 여러개의 웹소켓 연결이 가능하다.


프론트에 href 버튼에 axios 요청이벤트가 있으면이있으면
1. axios 요청
2. href 링크 요청 보냄
3. 소켓이 끝기던가 함
